# Template for NextJS environment variables.

# For local development, copy this file, rename it to .env.local, and fill in the values.
# When deploying live, you'll need to store the vars in Vercel/System config.

# If not set, we provide default values (check `scaffold.config.ts`) so developers can start prototyping out of the box,
# but we recommend getting your own API Keys for Production Apps.

# To access the values stored in this env file you can use: process.env.VARIABLENAME
# You'll need to prefix the variables names with NEXT_PUBLIC_ if you want to access them on the client side.
# More info: https://nextjs.org/docs/pages/building-your-application/configuring/environment-variables
NEXT_PUBLIC_ALCHEMY_API_KEY=
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# Lit Protocol Configuration
# Get your Lit Relay API key from: https://developer.litprotocol.com/
# For testnet (DatilDev), you can use 'test-api-key' for testing
# For production (Serrano), you must use your actual API key
NEXT_PUBLIC_LIT_RELAY_API_KEY=

# 32-byte hex key for encrypting session key pairs in opaque tokens (Lit delegation flow)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_TOKEN_SECRET=

STORAGE_GATEWAY_TYPE=LOCAL
INDEXER_GATEWAY_TYPE=PONDER
CIPHER_GATEWAY_TYPE=LOCAL
NEXT_PUBLIC_CIPHER_GATEWAY_TYPE=LOCAL
NEXT_PUBLIC_PONDER_URL=http://localhost:42069/graphql
NEXT_PUBLIC_LIT_LOGIN_URL=https://login.litgateway.com
LOCAL_STORAGE_PATH=./local-storage.json
SESSION_TOKEN_SECRET=d1dd08c0c804a761ffa9343c30c13368cf2e4f38c16616084add6401594d44a2